Drop the DIN-Geprüft Home Compostable mark here (request artwork + usage rules from DIN CERTCO or the PHA supplier;
do not copy it from the web), then set CERT.logoSrc in src/lib/certs.ts to "/certs/<filename>".
